# Secrets — never commit your real key
.env
.instructions.md

# Scraped output & debug snapshots
data/

# Scraped output & debug snapshots
data/

# OpenCode local state
.opencode/cache/
.opencode/state/

# OS / editor noise
.DS_Store
*.swp
.obsidian/
.logseq/

# Keep the memory/ folder, but if you'd rather not commit your agent's notes,
# uncomment the next line:
# memory/*.md

credentials.json
token.json
data/reviews/
